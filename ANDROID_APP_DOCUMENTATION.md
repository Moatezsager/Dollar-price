# وثيقة مواصفات تطبيق الأندرويد - مؤشر الدينار (Dinar Index)

## 📌 نظرة عامة على المشروع
"مؤشر الدينار" هو منصة مالية حية لتتبع أسعار صرف العملات الأجنبية والذهب في السوق الموازي والرسمي في ليبيا.
هذا الملف مخصص لمطور الأندرويد (Android Developer) ليكون دليلاً شاملاً لفهم هيكلة المشروع، البيانات، وآلية عمل التطبيق.

---

## 🏗️ هندسة النظام (Architecture)
التطبيق الحالي مبني بنظام **(Client-Server Architecture)**:
1. **الخادم (Backend - Node.js):** هو المسؤول **الوحيد** عن جلب الأسعار من قنوات التليجرام (Scraping)، وتحديث قاعدة البيانات، وإرسال الإشعارات.
2. **تطبيق الأندرويد (Client):** سيكون **تطبيقاً للقراءة فقط (Read-Only)**. 
   - 🚫 **لا يجب** أن يقوم تطبيق الأندرويد بأي عمليات سحب (Scraping) من التليجرام.
   - 🚫 **لا يجب** أن يقوم بالتعديل على قاعدة البيانات.
   - ✅ **وظيفته فقط:** جلب البيانات الجاهزة من قاعدة البيانات (أو الـ API) وعرضها للمستخدم بطريقة احترافية.

---

## 🗄️ مصادر البيانات (Data Sources)

مطور الأندرويد لديه خيارين لجلب البيانات (يُفضل دمج الخيارين معاً للحصول على أفضل تجربة):

### الخيار الأول: الاتصال المباشر بقاعدة بيانات Supabase (مُستحسن للتحديث اللحظي)
نحن نستخدم **Supabase** (بديل Firebase المفتوح المصدر مبني على PostgreSQL). يمكن استخدام مكتبة `supabase-kt` أو `supabase-flutter` لجلب البيانات والاستماع للتحديثات اللحظية (Real-time).

**المفاتيح المطلوبة (تأخذها من مدير المشروع):**
- **SUPABASE_URL:** رابط المشروع.
- **SUPABASE_ANON_KEY:** المفتاح العام (Public Key) للقراءة فقط.

**الجداول (Tables):**
1. **جدول `exchange_rates`:** 
   - يحتوي على صف واحد فقط (id = 1).
   - يخزن كائن (JSON) يحتوي على جميع الأسعار الحالية (الرسمية والموازية)، وتاريخ آخر تحديث، وأسعار الإغلاق السابقة.
2. **جدول `rate_history`:**
   - يخزن سجل الأسعار التاريخية (كل نصف ساعة) لرسم المخططات البيانية (Charts).
3. **جدول `price_changes_log`:**
   - سجل التغيرات (من ارتفع ومن انخفض) لإرسال الإشعارات.

### الخيار الثاني: استخدام REST API الخاص بالخادم (Node.js)
إذا لم يرغب المطور بربط Supabase مباشرة، يمكنه استدعاء هذه الروابط (Endpoints) من السيرفر الخاص بنا:

- **جلب الأسعار الحالية:** `GET https://[SERVER_URL]/api/rates`
  - *ترجع كائن JSON يحتوي على (official, parallel, previousOfficial, previousParallel, lastUpdated, lastChanged).*
- **جلب السجل التاريخي (للرسم البياني):** `GET https://[SERVER_URL]/api/history`
  - *ترجع مصفوفة من النقاط الزمنية والأسعار لآخر 24-48 ساعة.*
- **جلب آخر التغيرات:** `GET https://[SERVER_URL]/api/recent-changes`

---

## ✨ المميزات المطلوبة في تطبيق الأندرويد (Features)

1. **واجهة المستخدم (UI/UX):**
   - تصميم غامق (Dark Mode) بأسلوب الزجاج (Glassmorphism).
   - استخدام ألوان مضيئة (Neon): الأخضر للارتفاع 🟢، الأحمر للهبوط 🔴.
   - تأثيرات نبض (Pulse/Flash) عند تغير السعر.

2. **الأقسام الرئيسية:**
   - **الدولار كاش (USD Cash):** بطاقة رئيسية كبيرة تبرز سعر الدولار في السوق الموازي.
   - **الدولار صكوك (USD Checks):** بطاقة بارزة لسعر الصكوك.
   - **العملات الأجنبية:** (يورو، باوند، دينار تونسي، ليرة تركية، إلخ).
   - **صكوك المصارف:** تفصيل صكوك التجارة والتنمية، الوحدة، الأمان، إلخ (USD_NCB, USD_JBANK...).
   - **حوالات الخارج:** حوالات تركيا، دبي، الصين (USD_TR, USD_AE, USD_CN).
   - **الذهب:** أسعار الذهب الكسر والمشغول (عيار 18، 21، 24) والفضة.

3. **التحليل والرسوم البيانية (Charts):**
   - رسم بياني خطي (Line/Area Chart) يعرض حركة (الدولار كاش، دولار صكوك، يورو، ذهب كسر) خلال (أسبوع، شهر، 6 أشهر، سنة).
   - حساب (أعلى سعر، أقل سعر، متوسط السعر).

4. **مشاركة الأسعار (Share Image):**
   - عند الضغط على زر مشاركة في أي كارت سعر، يجب أن يقوم التطبيق بتوليد صورة (Bitmap/Canvas) تحتوي على تصميم جذاب للسعر مع إحصائيات 24 ساعة، ثم يفتح نافذة المشاركة الخاصة بالنظام (Share Intent).

5. **الإشعارات اللحظية (Push Notifications):**
   - يجب ربط التطبيق بـ Firebase Cloud Messaging (FCM) أو استخدام Supabase Realtime لإرسال إشعار للمستخدم عند تغير السعر الجذري.

---

## 🔐 الأمان والصلاحيات
- لا تقم بتضمين أي مفاتيح خاصة (Secret Keys) أو مفاتيح تليجرام (API Hash/API ID) في كود الأندرويد نهائياً.
- الاتصال بقاعدة البيانات يجب أن يكون بمفتاح (Anon Key) الذي يسمح بـ (SELECT) فقط.

---

## 📝 ملاحظة للمطور
التطبيق يعتمد بشكل أساسي على عرض البيانات التي يتم تحديثها من قبل السيرفر. كل المنطق المعقد (تحليل النصوص من التليجرام، معالجة الأخطاء، حساب التغيرات) مكتوب بالفعل في السيرفر وستجده جاهزاً في قاعدة البيانات. 
مهمتك هي بناء واجهة سريعة، تفاعلية، وجميلة! بالتوفيق 🚀
