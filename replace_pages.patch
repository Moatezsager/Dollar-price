1957c1957,1961
<         {currentPage === 'api' ? (
---
>         {currentPage === 'api' ? (
>           <Developers key="api" />
>         ) : currentPage === 'terms' ? (
>           <Terms key="terms" onBack={() => setCurrentPage('dashboard')} />
>         ) : currentPage === 'privacy' ? (
>           <Privacy key="privacy" onBack={() => setCurrentPage('dashboard')} />
>         ) : currentPage === 'contact' ? (
